<!-- Generated by tools/generate_protocol.py. Do not edit. -->
# Relay protocol reference

Schema version: `35` · Native methods: `127` · MCP tools: `111`

| Native method | MCP tool | Parameters | Access | Description |
| --- | --- | --- | --- | --- |
| `runtime.status` | `runtime_status` | — | read-only | Read the current editor or game mode, pause, frame, simulation time and resolution state. |
| `runtime.play` | `runtime_play` | — | mutating | Start a temporary game session from the authored scene. Stop restores the authored scene and discards runtime changes. |
| `runtime.stop` | `runtime_stop` | — | mutating | Stop the current game session and restore the authored scene without changing undo history. |
| `runtime.pause` | `runtime_pause` | — | mutating | Pause a running game so its scene can be inspected deterministically. |
| `runtime.resume` | `runtime_resume` | — | mutating | Resume a paused game after inspection or a controlled frame step. |
| `runtime.step` | `runtime_step` | `frames`: integer = `1` | mutating | Advance an exact number of deterministic game frames, including while paused. |
| `runtime.quit` | `runtime_shutdown` | — | mutating | Request an orderly shutdown of the runtime owned by this MCP bridge. |
| `editor.camera.status` | `editor_camera_status` | — | read-only | Read the live editor inspection viewpoint used by Vulkan captures. Unavailable without an editor. Does not modify scene cameras. |
| `editor.camera.set` | `editor_camera_set` | `target_x`: number<br>`target_y`: number<br>`target_z`: number<br>`yaw`: number<br>`pitch`: number<br>`distance`: number<br>`mode`: string = `"inspector"` | mutating | Set the live inspector camera target, orbit angles in radians and distance for visual confirmation using render_capture source vulkan. View-only: no scene or undo changes. |
| `editor.camera.frame` | `editor_camera_frame` | `entity`: string (required) | mutating | Frame an entity and its descendant bounds in the live inspection view before Vulkan capture. Does not change selection, scene cameras or undo history. |
| `render.capture` | `render_capture` | `filename`: string = `"agent-capture.png"`<br>`source`: string = `"vulkan"` | mutating | Save the current rendered frame in Relay's captures directory for visual inspection. |
| `render.capture_async` | `render_capture_async` | `filename`: string = `"agent-async.png"`<br>`source`: string | mutating | Capture real Vulkan or deterministic CPU frames through bounded background image workers. |
| `render.capture_cancel` | `render_capture_cancel` | `job`: integer (required) | mutating | Cancel a queued image or GPU readback job; writing and completed jobs cannot be cancelled. GPU slots remain alive until their fence completes. |
| `render.capture_status` | `render_capture_status` | `job`: integer (required) | read-only | Check whether an asynchronous capture is queued, writing, complete or failed. |
| `render.capabilities` | `render_capabilities` | — | read-only | Create a temporary Vulkan device and report adapter and modern rendering support. |
| `render.graph` | `render_graph` | — | read-only | Read compiled render passes, resources, dependencies and access transitions. |
| `render.shader_interfaces` | `render_shader_interfaces` | — | read-only | Read SPIR-V-reflected stages, locations, descriptor bindings and push-constant sizes from the live Vulkan pipeline. |
| `render.upload_status` | `render_upload_status` | — | read-only | Read Vulkan asset upload budgets, staging peaks, estimated resident bytes and transfer-path state. |
| `render.assets` | `render_assets` | — | read-only | List built-in and imported meshes, materials and textures currently available to scene renderer components. |
| `assets.formats` | `asset_import_formats` | — | read-only | Report model formats and feature coverage available in this Relay build, including Godot-compatible interchange paths. |
| `assets.import_model` | `asset_import_model` | `filename`: string (required)<br>`instantiate`: boolean = `true`<br>`preset`: string = `"scene"` | mutating | Import a model from the project-local assets directory using a content-addressed identity and optionally instantiate its node hierarchy. |
| `assets.browse` | `asset_browse` | `directory`: string = `""` | read-only | List the folders and files in one project folder, folders first, with each entry's asset kind. Hidden entries and symlinks are omitted; importable models are marked. |
| `assets.search` | `asset_search` | `query`: string = `""`<br>`kinds`: string_array | read-only | Find project files and folders anywhere below the project root whose names contain the query, optionally limited to asset kinds. Hidden entries and symlinks are skipped; at most 512 results. |
| `assets.create_folder` | `asset_create_folder` | `path`: string (required) | mutating | Create a new, empty folder inside an existing project folder. |
| `assets.move` | `asset_move` | `from`: string (required)<br>`to`: string (required) | mutating | Rename or move a project file or folder without overwriting. Import records follow moved models; project scene files and the project file cannot move. |
| `assets.delete` | `asset_delete` | `path`: string (required) | mutating, destructive | Move a project file or folder into the hidden .relay-trash folder, where it can be recovered by hand. Project scene files and the project file cannot be deleted. |
| `logs.read` | `logs_read` | `after`: integer = `0` | read-only | Read structured engine log entries newer than a sequence number. |
| `performance.read` | `performance_read` | `afterFrame`: integer = `0`<br>`limit`: integer = `30` | read-only | Read bounded per-frame CPU/GPU timing, draw/resource counts, entities and process memory. |
| `input.recent` | `input_recent` | — | read-only | Read the bounded normalized keyboard, mouse and gamepad input event history. |
| `input.map` | `input_map` | — | read-only | Read the project's input map: named actions bound to keys, mouse buttons or gamepad buttons, and axes built from button pairs or gamepad sticks. Also returns the engine defaults. Controls look like key:space, key:left_shift, mouse:left, gamepad:a or gamepad:leftx. |
| `input.set_map` | `input_set_map` | `map`: string (required) | mutating, destructive | Validate, save as input.relay-input.json in the project and apply a complete input map document (format relay.input, version 1) as returned by input.map. |
| `input.state` | `input_state` | — | read-only | Read the current game step's input: each action's held and pressed state, each axis value, held controls and mouse movement. |
| `input.simulate` | `input_simulate` | `name`: string (required)<br>`value`: number = `1`<br>`frames`: integer = `1` | mutating | Hold an action, or set an axis value, for a number of game steps during Run Game, as if a player pressed it. Use it with runtime.step to play-test scripts. |
| `input.release` | — | — | mutating, host only (no MCP tool) | Host-only. Release every held control, used when the game viewport loses input focus. |
| `video.start` | `video_start` | `filename`: string (required)<br>`fps`: integer = `30`<br>`maximumFrames`: integer = `300`<br>`source`: string | mutating | Record real Vulkan or deterministic CPU frames to WebM with explicit frame drops. |
| `video.capabilities` | `video_capabilities` | — | read-only | Report whether this Relay build found the FFmpeg WebM encoder. |
| `video.stop` | `video_stop` | — | mutating | Drain pending readbacks and start background WebM finalization; poll video.status for completion and errors. |
| `video.status` | `video_status` | — | read-only | Read recording state, submitted frames and explicitly reported frame drops. |
| `scene.list` | `scene_list` | — | read-only | List every live entity with its name, parent and local transform. |
| `scene.inspect` | `scene_inspect` | `entity`: string (required) | read-only | Inspect one entity using its stable generation-checked handle. |
| `scene.create` | `scene_create` | `name`: string<br>`parent`: string<br>`type`: string | mutating | Create a node, optionally of a node type from nodes.types (which gives it that type's components, inherited from its ancestors) and optionally parented to another live node. |
| `scene.destroy` | `scene_destroy` | `entity`: string (required) | mutating, destructive | Destroy an entity and its descendants as one undoable transaction. |
| `scene.duplicate` | `scene_duplicate` | `entity`: string (required) | mutating | Copy an entity and its descendants beside the original as one undoable transaction. Components are preserved, a copied camera is never the active one, and a copy of a whole imported model drives its own animation. |
| `scene.clear` | `scene_clear` | — | mutating, destructive | Destroy every entity as one undoable transaction, leaving an empty scene to start new work in. |
| `scene.set_transform` | `scene_set_transform` | `entity`: string (required)<br>`px`: number<br>`py`: number<br>`pz`: number<br>`rx`: number<br>`ry`: number<br>`rz`: number<br>`sx`: number<br>`sy`: number<br>`sz`: number<br>`gesture`: integer = `0` | mutating | Update selected local position, Euler rotation or scale fields in one transaction. |
| `scene.set_camera` | `scene_set_camera` | `entity`: string (required)<br>`enabled`: boolean = `true`<br>`active`: boolean = `true`<br>`fieldOfViewY`: number = `60`<br>`nearPlane`: number = `0.1`<br>`farPlane`: number = `1000`<br>`orthographicHeight`: number<br>`exposureEv`: number | mutating | Add, update or remove a perspective or orthographic camera; activating one deactivates the previous camera. |
| `scene.set_renderer` | `scene_set_renderer` | `entity`: string (required)<br>`enabled`: boolean = `true`<br>`mesh`: string = `"builtin.triangle"`<br>`material`: string = `"builtin.orange"` | mutating | Attach a registered built-in or imported mesh and material to an entity, or remove its renderer component. |
| `scene.set_parent` | `scene_set_parent` | `entity`: string (required)<br>`parent`: string or null (required) | mutating | Reparent an entity safely; use null to move it to the scene root. |
| `scene.undo` | `scene_undo` | — | mutating | Undo the most recent scene transaction and restore exact entity generations. |
| `scene.redo` | `scene_redo` | — | mutating | Reapply the most recently undone scene transaction. |
| `scene.set_animation` | `scene_set_animation` | `entity`: string (required)<br>`clip`: integer<br>`playing`: boolean<br>`loop`: boolean<br>`speed`: number<br>`timeSeconds`: number<br>`gesture`: integer | mutating | Configure clip, playback, looping, speed and seek time on an imported model root. |
| `scene.set_morph` | `scene_set_morph` | `entity`: string (required)<br>`target`: integer = `0`<br>`weight`: number = `0`<br>`reset`: boolean = `false` | mutating | Override a mesh morph weight, or reset all overrides to imported defaults and animation. |
| `scene.set_collider` | `scene_set_collider` | `entity`: string (required)<br>`attached`: boolean<br>`enabled`: boolean<br>`type`: string<br>`centerX`: number<br>`centerY`: number<br>`centerZ`: number<br>`halfX`: number<br>`halfY`: number<br>`halfZ`: number<br>`radius`: number<br>`halfHeight`: number<br>`mesh`: string<br>`layer`: integer<br>`mask`: integer | mutating | Add, edit or remove an authored box, sphere, capsule, convex or triangle-mesh collider. Sphere and capsule use the largest world scale uniformly. Convex and mesh shapes use a registered mesh, defaulting to the entity renderer mesh. Undoable and saved with the scene. |
| `physics.raycast` | `physics_raycast` | `originX`: number (required)<br>`originY`: number (required)<br>`originZ`: number (required)<br>`directionX`: number (required)<br>`directionY`: number (required)<br>`directionZ`: number (required)<br>`maximumDistance`: number<br>`layerMask`: integer | read-only | Find the nearest enabled authored collider of any shape hit by a world-space ray. Returns hit point and surface normal; does not move objects. |
| `physics.overlaps` | `physics_overlaps` | `entity`: string (required) | read-only | List up to 128 enabled colliders of any shape overlapping an entity collider. Both colliders must pass their layer masks; reports truncation. Triangle-mesh colliders report surface contact only. |
| `physics.debug_boxes` | — | — | read-only, host only (no MCP tool) | Read bounded world-space collider outlines for the editor overlay, including disabled colliders. |
| `physics.body_status` | `physics_body_status` | `entity`: string (required) | read-only | Read the current linear and angular velocities of a dynamic body during Run Game. |
| `physics.contact_events` | `physics_contact_events` | `after`: integer = `0` | read-only | Read bounded contact begin and end events from the current game session, using a sequence cursor. Events are cleared on Stop Game. |
| `physics.apply_impulse` | `physics_apply_impulse` | `entity`: string (required)<br>`impulseX`: number (required)<br>`impulseY`: number (required)<br>`impulseZ`: number (required)<br>`pointX`: number<br>`pointY`: number<br>`pointZ`: number | mutating | Apply a world-space impulse to a dynamic body during Run Game. Optional world-space point produces torque and angular motion. |
| `scene.set_physics_body` | `scene_set_physics_body` | `entity`: string (required)<br>`attached`: boolean<br>`type`: string<br>`mass`: number<br>`gravityScale`: number<br>`restitution`: number<br>`friction`: number<br>`linearDamping`: number<br>`angularDamping`: number<br>`lockRotation`: boolean | mutating | Add, edit or remove an undoable static or dynamic physics body. Dynamic bodies use Jolt rigid-body collision, gravity and angular dynamics during Run Game. |
| `scene.set_first_person_controller` | `scene_set_first_person_controller` | `entity`: string (required)<br>`attached`: boolean<br>`walkSpeed`: number<br>`sprintSpeed`: number<br>`jumpSpeed`: number<br>`mouseSensitivity`: number<br>`stickLookSpeed`: number<br>`invertY`: boolean<br>`groundDistance`: number<br>`camera`: string | mutating | Add, edit or remove an undoable first person controller component. During Run Game it looks around with the mouse and look_x/look_y through a child camera node, walks with move_x/move_y relative to the view, sprints and jumps (only on the ground). It needs a dynamic physics body with rotation locked and a collider; scene.create with type FirstPersonController sets all of that up. |
| `scripts.status` | `scripts_status` | — | read-only | Read native C++ script state: project trust, build state, whether sources changed since the last build, registered behaviours, compiler diagnostics with file and line, and runtime errors from behaviour callbacks. |
| `scripts.build` | `scripts_build` | — | mutating | Compile the project's scripts/ C++ files into a native library in the background; unchanged files are reused. Poll scripts.status until the state is ready or failed. A build that finishes during Run Game hot reloads the running behaviours. Requires a project a person has trusted. |
| `scripts.trust` | — | `trusted`: boolean (required) | mutating, host only (no MCP tool) | Host-only. Allow or forbid building and running this project's native C++ scripts, which run with the editor's full privileges. Stored per user, outside the project. |
| `scripts.sdk` | `scripts_sdk` | — | read-only | Return relay_script.hpp, the complete C++ API available to gameplay scripts, with usage notes and frame order. Read it before writing a script. |
| `scripts.read` | `scripts_read` | `path`: string (required) | read-only | Read a C++ source or header from the project's scripts/ folder. |
| `scripts.write` | `scripts_write` | `path`: string (required)<br>`source`: string (required) | mutating, destructive | Create or replace a C++ source or header in the project's scripts/ folder, creating subfolders. Allowed during Run Game; build afterwards to compile it or hot reload it. |
| `scripts.create` | `scripts_create` | `behaviour`: string (required) | mutating | Create scripts/<behaviour>.cpp from a starter template defining and registering that behaviour class. Refuses to overwrite. |
| `component.types` | `component_types` | — | read-only | List every component a node can carry: engine components with their category and whether they can be added, removed or repeated, plus each built script behaviour with its declared properties and code defaults. |
| `component.add` | `component_add` | `entity`: string (required)<br>`component`: string (required)<br>`behaviour`: string | mutating | Add an engine component with editor defaults, or append a script component running a behaviour, as one undoable transaction. Configure it afterwards with the component's own method, such as scene.set_camera or scene.set_script_property. |
| `component.remove` | `component_remove` | `entity`: string (required)<br>`component`: string (required)<br>`index`: integer | mutating, destructive | Remove one component as an undoable transaction. The Transform and imported model animation cannot be removed. |
| `scene.set_script` | `scene_set_script` | `entity`: string (required)<br>`index`: integer (required)<br>`behaviour`: string<br>`enabled`: boolean | mutating | Change the behaviour or enabled state of one of a node's script components, as an undoable transaction. |
| `scene.set_script_property` | `scene_set_script_property` | `entity`: string (required)<br>`index`: integer (required)<br>`property`: string (required)<br>`number`: number<br>`boolean`: boolean<br>`text`: string<br>`vector`: number_array<br>`reset`: boolean = `false` | mutating | Store a value for a behaviour property on one script component, or reset it to the code default. Send exactly one of number, boolean, text or vector unless resetting. |
| `nodes.types` | `nodes_types` | — | read-only | List the node type tree. Each type adds components to its parent's; creatable types can be passed to scene.create. A node's reported type is the deepest type whose components it has. |
| `templates.list` | `templates_list` | — | read-only | List the project's saved templates (prefabs) with the node type of each root. Built-in node types are listed by nodes.types. |
| `templates.instantiate` | `templates_instantiate` | `template`: string (required)<br>`parent`: string<br>`name`: string | mutating | Create a saved node tree from a project template as one undoable transaction and return its root. The result is an independent copy. |
| `templates.save` | `templates_save` | `entity`: string (required)<br>`name`: string (required)<br>`replace`: boolean = `false` | mutating, destructive | Save a node and its children as a project template in templates/<name>.relay-template.json, usable from the Create menu and templates.instantiate. |
| `scene.set_light` | `scene_set_light` | `entity`: string (required)<br>`enabled`: boolean = `true`<br>`type`: string<br>`red`: number<br>`green`: number<br>`blue`: number<br>`intensity`: number<br>`constant`: number<br>`linear`: number<br>`quadratic`: number<br>`innerCone`: number<br>`outerCone`: number<br>`range`: number | mutating | Add, update or remove a directional, point or spot light. |
| `scene.rename` | `scene_rename` | `entity`: string (required)<br>`name`: string (required) | mutating | Change an entity's display name as one undoable transaction. |
| `scene.history` | `scene_history` | — | read-only | Read the labels currently on the undo and redo stacks, newest first. |
| `assets.available` | `asset_available_models` | — | read-only | List model files present in the project assets directory that this build can import. Top-level files only; the import sandbox is unchanged. |
| `scene.pick` | `scene_pick` | `originX`: number (required)<br>`originY`: number (required)<br>`originZ`: number (required)<br>`directionX`: number (required)<br>`directionY`: number (required)<br>`directionZ`: number (required) | read-only | Find the nearest drawable entity a world-space ray enters. Bounds-level precision, not per-triangle. Stateless: the caller supplies the ray, so the engine stores no viewpoint. |
| `scene.bounds` | `scene_bounds` | `entity`: string (required) | read-only | Read the world-space axis-aligned bounds and origin of an entity and its descendants, for framing a selection or locating an object. |
| `scene.snapshot` | `scene_snapshot` | — | read-only | Return deterministic scene JSON plus reflected component field metadata. |
| `scene.save` | `scene_save` | `filename`: string (required) | mutating, destructive | Atomically save the current scene in Relay's project-local scenes directory. |
| `scene.load` | `scene_load` | `filename`: string (required) | mutating | Validate, migrate and load a project scene as one undoable transaction. |
| `trace.start` | `trace_start` | `filename`: string (required) | mutating | Record frame-stamped agent commands and normalized human input events. |
| `trace.stop` | `trace_stop` | — | mutating | Atomically persist the active deterministic trace in Relay's traces directory. |
| `trace.status` | `trace_status` | — | read-only | Read trace recording state, path and current event count. |
| `trace.replay` | `trace_replay` | `filename`: string (required) | mutating | Validate and replay a trace against the fixed-step runtime at its recorded frame offsets. |
| `scene.copy` | `scene_copy` | `entities`: string_array (required) | mutating | Copy selected subtrees to the session clipboard without changing the scene. |
| `scene.cut` | `scene_cut` | `entities`: string_array (required) | mutating, destructive | Copy and remove selected subtrees as one undoable edit. |
| `scene.duplicate_many` | `scene_duplicate_many` | `entities`: string_array (required) | mutating | Duplicate selected subtrees beside their originals as one undoable edit. |
| `scene.destroy_many` | `scene_destroy_many` | `entities`: string_array (required) | mutating, destructive | Remove selected subtrees as one undoable edit. |
| `scene.transform_many` | `scene_transform_many` | `entities`: string_array (required)<br>`delta`: number_array (required)<br>`gesture`: integer | mutating | Apply a column-major world-space affine delta matrix to selected roots as one undoable gesture. |
| `scene.paste` | `scene_paste` | `parent`: string or null | mutating | Paste the session clipboard as one undoable edit; cameras stay inactive and internal model bindings are remapped. |
| `scene.clipboard` | `scene_clipboard` | — | read-only | Read session clipboard root and entity counts. |
| `project.create` | `project_create` | `filename`: string (required)<br>`name`: string (required) | mutating | Create a folder project (.relayproject) and an empty scene; refuses to overwrite an existing project. |
| `project.open` | `project_open` | `filename`: string (required) | mutating | Open a folder project (.relayproject) and load its startup scene atomically, or clear the scene for an empty project. |
| `project.status` | `project_status` | — | read-only | Inspect the current project, member scene files and startup scene. |
| `project.list` | `project_list` | — | read-only | List available workspace project files. |
| `project.add_scene` | `project_add_scene` | `sceneFile`: string (required) | mutating | Add an existing valid scene file to the current project; the first scene becomes its startup scene. |
| `project.remove_scene` | `project_remove_scene` | `sceneFile`: string (required) | mutating | Remove scene membership without deleting its file; chooses a remaining startup scene when needed. |
| `project.set_startup` | `project_set_startup` | `sceneFile`: string (required) | mutating | Choose a current project member as the startup scene. |
| `project.close` | `project_close` | — | mutating | Close project metadata while leaving the current scene intact. |
| `project.package` | `project_package` | `filename`: string (required) | mutating | Export saved project metadata, member scenes and non-hidden assets as a portable uncompressed tar under the project exports directory. Refuses to overwrite; save scene changes first. |
| `scene.keyframe.set` | `scene_keyframe_set` | `entity`: string (required)<br>`timeSeconds`: number (required)<br>`px`: number<br>`py`: number<br>`pz`: number<br>`rx`: number<br>`ry`: number<br>`rz`: number<br>`sx`: number<br>`sy`: number<br>`sz`: number<br>`gesture`: integer | mutating | Add or update a scene-owned transform keyframe at an exact time. Unspecified transform fields use the current entity transform or existing key. Undoable and saved with the scene. |
| `scene.keyframe.delete` | `scene_keyframe_delete` | `entity`: string (required)<br>`timeSeconds`: number (required) | mutating | Remove one scene-owned transform keyframe at an exact time as an undoable edit. |
| `scene.keyframes.playback` | `scene_keyframes_playback` | `entity`: string (required)<br>`playing`: boolean<br>`loop`: boolean<br>`speed`: number<br>`durationSeconds`: number<br>`timeSeconds`: number<br>`gesture`: integer | mutating | Set playback, loop, speed, duration or current time for scene-owned transform keys. Undoable and saved with the scene. |
| `scene.set_animations` | `scene_set_animations` | `entities`: string_array (required)<br>`playing`: boolean<br>`loop`: boolean<br>`speed`: number<br>`timeSeconds`: number<br>`gesture`: integer | mutating | Seek or configure multiple animation roots in one undoable gesture; time clamps to each clip duration. |
| `animation.clip` | `animation_clip` | `model`: string (required)<br>`clip`: integer (required) | read-only | Read bounded channel key times for an imported animation clip. Key times are read-only; truncated channels report their full key count. |
| `session.status` | `session_status` | — | read-only | Inspect exact native method grants for this connection. Grants are approved only by the host; no tool can expand them. |
| `session.audit` | `session_audit` | `after`: integer = `0` | read-only | Read bounded session action decisions and outcomes. Scope is the exact native method. Oldest sequence exposes eviction; parameters are not retained. |
| `session.request` | `session_request` | `scope`: string (required)<br>`kind`: string = `"method"`<br>`target`: string = `""` | mutating | Request host approval for an exact method and optional entity/file target. This never grants access or executes an action. |
| `session.review` | — | — | read-only, host only (no MCP tool) | Inspect pending requests, active scoped grants and native action descriptions. |
| `session.grant` | — | `scope`: string (required)<br>`kind`: string = `"method"`<br>`target`: string = `""` | mutating, host only (no MCP tool) | Host approval of an exact method scope bound to the current project. |
| `session.decide` | — | `request`: integer (required)<br>`allow`: boolean (required) | mutating, host only (no MCP tool) | Host approval or denial of a pending request. Stale project requests cannot be approved. |
| `session.revoke` | — | `scope`: string = `""` | mutating, host only (no MCP tool) | Host revocation of all grants or one exact method. |
| `session.export_audit` | — | `filename`: string (required) | mutating, host only (no MCP tool) | Export a bounded audit snapshot atomically under .relay/audits without overwriting existing records. |
| `chat.submit` | — | `message`: string (required)<br>`attachments`: string_array | mutating, host only (no MCP tool) | Submit a user message to the external bridge. Providers and canonical conversation history remain outside the engine. |
| `chat.status` | — | — | read-only, host only (no MCP tool) | Inspect the bounded external bridge display and pending user-message count. |
| `bridge.poll` | — | — | mutating, authenticated bridge only (no MCP tool) | Consume bounded user-message submissions for the authenticated external bridge. |
| `bridge.publish` | — | `view`: string (required) | mutating, authenticated bridge only (no MCP tool) | Publish a bounded display snapshot from the authenticated external bridge. |
| `chat.cancel` | — | — | mutating, host only (no MCP tool) | Cancel pending chat submissions and ask the external bridge to abort its active turn. Completed native actions remain undoable. |
| `session.auto_approval` | — | `enabled`: boolean (required) | mutating, host only (no MCP tool) | Set full session access to every public engine action without approval prompts. Trusted host only. |
| `chat.configure` | — | `endpoint`: string (required)<br>`model`: string (required)<br>`auth`: string (required)<br>`header`: string = `""`<br>`credential`: string = `""`<br>`clear`: boolean = `false` | mutating, host only (no MCP tool) | Pass transient provider settings to the external bridge for private storage. Never traced or exposed to agent tools. |
| `chat.control` | — | `action`: string (required)<br>`model`: string = `""`<br>`effort`: string = `""`<br>`provider`: string = `"openai"` | mutating, host only (no MCP tool) | Host-only OpenAI sign-in, account, model, reasoning and conversation controls, processed by the external bridge. |

## Generation

Edit `protocol/relay.protocol.json`, then run `python3 tools/generate_protocol.py`.
Use `python3 tools/generate_protocol.py --check` in CI to reject stale generated artifacts.
